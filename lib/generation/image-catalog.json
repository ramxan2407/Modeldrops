{
  "syncedAt": "2026-09-22T13:27:09.045Z",
  "source": "https://api.wavespeed.ai/api/v3/models",
  "images": [
    {
      "endpoint": "openai/gpt-image-2.5-flare/text-to-image",
      "category": "text-to-image",
      "basePrice": 0.024,
      "description": "OpenAI's GPT Image 2.5 Flare Text-to-Image generates high-quality images from natural-language prompts, with five quality tiers up to 4K. Flare is the fast, balanced GPT Image 2.5 tier for everyday generation at low latency. Ready-to-use REST inference API, best performance, no coldstarts, affordable pricing.",
      "schema": {
        "additionalProperties": false,
        "properties": {
          "aspect_ratio": {
            "description": "The aspect ratio of the generated image.",
            "enum": [
              "1:1",
              "1:2",
              "2:1",
              "1:3",
              "3:1",
              "2:3",
              "3:2",
              "3:4",
              "4:3",
              "4:5",
              "5:4",
              "9:16",
              "16:9",
              "9:21",
              "21:9"
            ],
            "type": "string",
            "x-placeholder": "Select aspect ratio"
          },
          "enable_base64_output": {
            "default": false,
            "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
            "disabled": true,
            "type": "boolean"
          },
          "enable_sync_mode": {
            "default": false,
            "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
            "disabled": true,
            "type": "boolean"
          },
          "output_format": {
            "default": "png",
            "description": "The format of the output image.",
            "enum": ["png", "jpeg", "webp"],
            "type": "string"
          },
          "prompt": {
            "description": "The positive prompt for the generation.",
            "type": "string"
          },
          "quality": {
            "default": "medium",
            "description": "Rendering quality tier. Higher tiers add detail and cost more: low is the fastest draft tier, medium is the balanced default, high and xhigh add fidelity, max is the highest-fidelity tier.",
            "enum": ["low", "medium", "high", "xhigh", "max"],
            "type": "string"
          },
          "resolution": {
            "default": "1k",
            "description": "The resolution of the output image.",
            "enum": ["1k", "2k", "4k"],
            "type": "string"
          }
        },
        "required": ["prompt"],
        "type": "object",
        "x-order-properties": [
          "prompt",
          "aspect_ratio",
          "resolution",
          "quality",
          "output_format",
          "enable_sync_mode",
          "enable_base64_output"
        ]
      }
    },
    {
      "endpoint": "openai/gpt-image-2.5-sunburst/edit",
      "category": "image-to-image",
      "basePrice": 0.039,
      "description": "OpenAI's GPT Image 2.5 Sunburst Edit edits one or more reference images from natural-language instructions, with five quality tiers up to 4K. Sunburst is the precision-focused GPT Image 2.5 tier that spends more time per image for extra fidelity on intricate detail. Ready-to-use REST inference API, best performance, no coldstarts, affordable pricing.",
      "schema": {
        "additionalProperties": false,
        "properties": {
          "aspect_ratio": {
            "description": "The aspect ratio of the generated image. Auto-detected from input image if not specified.",
            "enum": [
              "1:1",
              "1:2",
              "2:1",
              "1:3",
              "3:1",
              "2:3",
              "3:2",
              "3:4",
              "4:3",
              "4:5",
              "5:4",
              "9:16",
              "16:9",
              "9:21",
              "21:9"
            ],
            "type": "string",
            "x-placeholder": "Select aspect ratio"
          },
          "enable_base64_output": {
            "default": false,
            "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
            "disabled": true,
            "type": "boolean"
          },
          "enable_sync_mode": {
            "default": false,
            "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
            "disabled": true,
            "type": "boolean"
          },
          "images": {
            "description": "List of URLs of input images for editing.",
            "items": {
              "type": "string"
            },
            "maxItems": 16,
            "type": "array"
          },
          "output_format": {
            "default": "png",
            "description": "The format of the output image.",
            "enum": ["png", "jpeg", "webp"],
            "type": "string"
          },
          "prompt": {
            "description": "The positive prompt for the generation.",
            "type": "string"
          },
          "quality": {
            "default": "medium",
            "description": "Rendering quality tier. Higher tiers add detail and cost more: low is the fastest draft tier, medium is the balanced default, high and xhigh add fidelity, max is the highest-fidelity tier.",
            "enum": ["low", "medium", "high", "xhigh", "max"],
            "type": "string"
          },
          "resolution": {
            "default": "1k",
            "description": "The resolution of the output image.",
            "enum": ["1k", "2k", "4k"],
            "type": "string"
          }
        },
        "required": ["prompt", "images"],
        "type": "object",
        "x-order-properties": [
          "prompt",
          "images",
          "aspect_ratio",
          "resolution",
          "quality",
          "output_format",
          "enable_sync_mode",
          "enable_base64_output"
        ]
      }
    },
    {
      "endpoint": "openai/gpt-image-2.5-sunburst/text-to-image",
      "category": "text-to-image",
      "basePrice": 0.024,
      "description": "OpenAI's GPT Image 2.5 Sunburst Text-to-Image generates high-quality images from natural-language prompts, with five quality tiers up to 4K. Sunburst is the precision-focused GPT Image 2.5 tier that spends more time per image for extra fidelity on intricate detail. Ready-to-use REST inference API, best performance, no coldstarts, affordable pricing.",
      "schema": {
        "additionalProperties": false,
        "properties": {
          "aspect_ratio": {
            "description": "The aspect ratio of the generated image.",
            "enum": [
              "1:1",
              "1:2",
              "2:1",
              "1:3",
              "3:1",
              "2:3",
              "3:2",
              "3:4",
              "4:3",
              "4:5",
              "5:4",
              "9:16",
              "16:9",
              "9:21",
              "21:9"
            ],
            "type": "string",
            "x-placeholder": "Select aspect ratio"
          },
          "enable_base64_output": {
            "default": false,
            "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
            "disabled": true,
            "type": "boolean"
          },
          "enable_sync_mode": {
            "default": false,
            "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
            "disabled": true,
            "type": "boolean"
          },
          "output_format": {
            "default": "png",
            "description": "The format of the output image.",
            "enum": ["png", "jpeg", "webp"],
            "type": "string"
          },
          "prompt": {
            "description": "The positive prompt for the generation.",
            "type": "string"
          },
          "quality": {
            "default": "medium",
            "description": "Rendering quality tier. Higher tiers add detail and cost more: low is the fastest draft tier, medium is the balanced default, high and xhigh add fidelity, max is the highest-fidelity tier.",
            "enum": ["low", "medium", "high", "xhigh", "max"],
            "type": "string"
          },
          "resolution": {
            "default": "1k",
            "description": "The resolution of the output image.",
            "enum": ["1k", "2k", "4k"],
            "type": "string"
          }
        },
        "required": ["prompt"],
        "type": "object",
        "x-order-properties": [
          "prompt",
          "aspect_ratio",
          "resolution",
          "quality",
          "output_format",
          "enable_sync_mode",
          "enable_base64_output"
        ]
      }
    },
    {
      "endpoint": "openai/gpt-image-2.5-flare/edit",
      "category": "image-to-image",
      "basePrice": 0.039,
      "description": "OpenAI's GPT Image 2.5 Flare Edit edits one or more reference images from natural-language instructions, with five quality tiers up to 4K. Flare is the fast, balanced GPT Image 2.5 tier for everyday generation at low latency. Ready-to-use REST inference API, best performance, no coldstarts, affordable pricing.",
      "schema": {
        "additionalProperties": false,
        "properties": {
          "aspect_ratio": {
            "description": "The aspect ratio of the generated image. Auto-detected from input image if not specified.",
            "enum": [
              "1:1",
              "1:2",
              "2:1",
              "1:3",
              "3:1",
              "2:3",
              "3:2",
              "3:4",
              "4:3",
              "4:5",
              "5:4",
              "9:16",
              "16:9",
              "9:21",
              "21:9"
            ],
            "type": "string",
            "x-placeholder": "Select aspect ratio"
          },
          "enable_base64_output": {
            "default": false,
            "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
            "disabled": true,
            "type": "boolean"
          },
          "enable_sync_mode": {
            "default": false,
            "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
            "disabled": true,
            "type": "boolean"
          },
          "images": {
            "description": "List of URLs of input images for editing.",
            "items": {
              "type": "string"
            },
            "maxItems": 16,
            "type": "array"
          },
          "output_format": {
            "default": "png",
            "description": "The format of the output image.",
            "enum": ["png", "jpeg", "webp"],
            "type": "string"
          },
          "prompt": {
            "description": "The positive prompt for the generation.",
            "type": "string"
          },
          "quality": {
            "default": "medium",
            "description": "Rendering quality tier. Higher tiers add detail and cost more: low is the fastest draft tier, medium is the balanced default, high and xhigh add fidelity, max is the highest-fidelity tier.",
            "enum": ["low", "medium", "high", "xhigh", "max"],
            "type": "string"
          },
          "resolution": {
            "default": "1k",
            "description": "The resolution of the output image.",
            "enum": ["1k", "2k", "4k"],
            "type": "string"
          }
        },
        "required": ["prompt", "images"],
        "type": "object",
        "x-order-properties": [
          "prompt",
          "images",
          "aspect_ratio",
          "resolution",
          "quality",
          "output_format",
          "enable_sync_mode",
          "enable_base64_output"
        ]
      }
    },
    {
      "endpoint": "wavespeed-ai/qwen-image/edit",
      "category": "image-to-image",
      "basePrice": 0.02,
      "description": "Qwen-Image-Edit is a 20B MMDiT image-to-image model offering precise bilingual (Chinese & English) text edits while preserving style. Ready-to-use REST inference API, best performance, no coldstarts, affordable pricing.",
      "schema": {
        "additionalProperties": false,
        "properties": {
          "enable_base64_output": {
            "default": false,
            "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
            "disabled": true,
            "type": "boolean"
          },
          "enable_sync_mode": {
            "default": false,
            "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
            "disabled": true,
            "type": "boolean"
          },
          "image": {
            "description": "The image to generate an image from.",
            "type": "string"
          },
          "output_format": {
            "default": "jpeg",
            "description": "The format of the output image.",
            "enum": ["jpeg", "png", "webp"],
            "type": "string"
          },
          "prompt": {
            "description": "The positive prompt for the generation.",
            "type": "string"
          },
          "seed": {
            "description": "The random seed to use for the generation. -1 means a random seed will be used.",
            "type": "integer"
          },
          "size": {
            "description": "The size of the generated media in pixels (width*height).",
            "type": "string",
            "x-hidden": true
          }
        },
        "required": ["prompt", "image"],
        "type": "object",
        "x-order-properties": [
          "prompt",
          "image",
          "size",
          "seed",
          "output_format",
          "enable_base64_output",
          "enable_sync_mode"
        ]
      }
    }
  ]
}
